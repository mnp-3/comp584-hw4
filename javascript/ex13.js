let originalData = [10, 20, 30, 40];
let clonedData = originalData.slice();

console.log(clonedData);

//slice creates a copy of the array and then returns it
//be aware that when changes are made to the copy it will also change the original